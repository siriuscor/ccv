# light version
    - command line UI(https://www.npmjs.com/package/inquirer)
    - search via sites
    - pack(fail)

# electron version
    - search page
    - task management
    - follow series
    - setting